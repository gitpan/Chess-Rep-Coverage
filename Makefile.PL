#!/usr/bin/perl
use strict;
use warnings;
use ExtUtils::MakeMaker;
WriteMakefile(
    NAME => 'Chess::Rep::Coverage',
    VERSION_FROM => 'lib/Chess/Rep/Coverage.pm',
    PREREQ_PM => {
        'Chess::Rep' => 0,
    },
    ($] >= 5.005
        ? (ABSTRACT_FROM => 'lib/Chess/Rep/Coverage.pm',
           AUTHOR => 'Gene Boggs <gene@cpan.org>')
        : ()
    ),
);
